import { NgModule } from '@angular/core';
import { SyncErrorComponent } from './sync-error/sync-error';
@NgModule({
	declarations: [SyncErrorComponent],
	imports: [],
	exports: [SyncErrorComponent]
})
export class ComponentsModule {}
